package FIMS;

import gui.GUI;

/**
 * Handler for viewing all records
 * 
 */
public class ViewAllRecordsHandler
{
	/**
	 * View all records. Get all records from the datastore, pass them back to GUI
	 * to populate the list
	 * 
	 * @param frame
	 */
	public void viewAll(GUI frame)
	{
		// db.getAllRecords();
		// build arraylist to pass back to gui?
	}

}
